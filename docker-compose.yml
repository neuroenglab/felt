services:
  app:
    build:
      context: .
      args:
        - VITE_BASE_PATH=${VITE_BASE_PATH:-/}
    ports:
      - "5000:5000"
    environment:
      - ROOT_PATH=${ROOT_PATH:-}
    volumes:
      - ./logs:/app/logs